source 'https://rubygems.org'
gem 'github-pages', group: :jekyll_plugins

group :travis do
    gem 'html-proofer'
    gem 'travis'
end

group :jekyll_plugins do
    gem 'jekyll-scholar'
    gem 'unicode_utils'
    gem 'unicode'
end